export enum Robots {
    OpticalTable
}

export enum Models {
    OpticalTable
}

export enum ArmState {
    Idle,
    Moving,
}

export enum ArmCommand {
    GOTO_A,
    GOTO_B,
    STOP
}

export enum InputKey {
    ArrowUp = 'ArrowUp',
    ArrowDown = 'ArrowDown',
    ArrowLeft = 'ArrowLeft',
    ArrowRight = 'ArrowRight',
    Space = 'Space'
}

export enum Actions {
    PLAYER_MOVE
}

