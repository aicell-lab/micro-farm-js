export enum Robots {
    OpticalTable
}

export enum Models {
    OpticalTable
}

export enum ArmState {
    Idle,
    Moving,
}

export enum ArmCommand {
    GOTO_A,
    GOTO_B,
    STOP
}

export enum Actions {
    PLAYER_MOVE,
    PLAYER_ROTATE,
}

