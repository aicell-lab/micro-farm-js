import { Models } from "../types/models";

export const modelFilepaths: { [key in Models]: string } = {
    [Models.OpticalTable]: "objs/optical_table.obj",
};

export const assetPath = './assets.zip'

